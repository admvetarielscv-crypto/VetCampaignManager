# Vitest mode overrides — tests always run in localStorage mode.
# Vitest loads .env files (Vite behavior); .env.test takes priority over .env,
# so unit tests never depend on a developer's configured backend.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
